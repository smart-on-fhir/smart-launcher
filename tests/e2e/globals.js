const TIMEOUT      = 15000; // timeout when waiting for something
const PAUSE        = 800;   // pause between interactions so that humans can see wait is going on
const PICKER_URL   = "https://localhost:8443";

module.exports = {
    waitForConditionTimeout: TIMEOUT,
    TIMEOUT,
    PAUSE,
    PICKER_URL
};